import React from 'react';export function Switch({checked,onCheckedChange,disabled=false}: {checked:boolean;onCheckedChange:(value:boolean)=>void;disabled?:boolean}) {return <button type="button" role="switch" aria-label="Enable setting" aria-checked={checked} disabled={disabled} data-slot="switch" {...(checked?{'data-checked':''}:{'data-unchecked':''})} onClick={()=>onCheckedChange(!checked)}><span data-slot="switch-thumb"/></button>}
